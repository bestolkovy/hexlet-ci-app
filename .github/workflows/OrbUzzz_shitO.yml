name: Ou-rrrr-buZZZ

on: push

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18.x'
          cache: 'npm'
        
      - run: npm ci

      - name: Install dependencies
        run: npm install
            make setup
            make start
            make test
            make lint





